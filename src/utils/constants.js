/**
 * Application Constants
 */

// Official WhatsApp Bot & Support Phone Number
// Configurable via VITE_WHATSAPP_NUMBER in .env, with safe fallback default
export const SUPPORT_WHATSAPP_NUMBER = import.meta.env.VITE_WHATSAPP_NUMBER || '923411713517';
